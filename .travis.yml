language: ruby
sudo: required
cache: bundler
rvm:
  - 2.2.5
  - 2.3.1

before_install: gem install bundler -v 1.11.2

after_failure:
  - cat /tmp/processor.log
  - cat /tmp/crawler_out.log

services:
  - rabbitmq
